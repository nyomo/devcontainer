{
    "name": "Dev Container Template",
    "dockerComposeFile": "./docker-compose.yml",
    "service": "devcontainer",
    "workspaceFolder": "/workdir",
    "postCreateCommand": "sudo apt-get install -y --no-install-recommends gh curl ssh && git config --global --add safe.directory /workdir&&sudo ln -s /workdir/.codex /home/appuser" 
}